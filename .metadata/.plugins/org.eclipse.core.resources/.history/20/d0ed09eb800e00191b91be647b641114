package ie.gmit.sw;

import java.util.concurrent.BlockingQueue;

public class FileParser implements Runnable {
  private BlockingQueue<Word> queue;
  private String file;
  
  public public FileParser(BlockingQueue<Word> queue, String file) {
    this.queue = q;
    this.file = file;
  }

  @Override
  public void run() {
    
  }

}
